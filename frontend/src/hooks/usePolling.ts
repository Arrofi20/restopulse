// STUB — replaced in GREEN step (TDD RED).
// Real implementation: polling with Page Visibility API per RESEARCH.md §Pattern 5.
export function usePolling(_fetchFn: () => Promise<void>, _intervalMs: number): void {
  // no-op stub
}
