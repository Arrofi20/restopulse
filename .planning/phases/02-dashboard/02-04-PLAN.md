---
phase: 02-dashboard
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - frontend/src/hooks/usePolling.ts
  - frontend/src/hooks/useDashboard.ts
  - frontend/src/components/dashboard/DateFilter.tsx
  - frontend/src/components/dashboard/SummaryCards.tsx
  - frontend/src/pages/DashboardPage.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "DashboardPage fetches data on mount for default 7-day range (D-01)"
    - "DateFilter provides preset buttons: 7 Hari, 30 Hari, Bulan Ini, Semua (D-02)"
    - "DateFilter includes manual date picker inputs for custom range (D-03)"
    - "Date range filter is shared between Line Chart and Pie Chart (D-04)"
    - "SummaryCards display total revenue (Rupiah formatted) and transaction count (D-05)"
    - "usePolling fetches every 30s, pauses on tab hidden, resumes on tab visible (D-10, D-13)"
    - "Chart grid: side-by-side on desktop (lg:grid-cols-2), stacked on mobile (D-16)"
  artifacts:
    - path: "frontend/src/hooks/usePolling.ts"
      provides: "Generic polling hook with Page Visibility API"
      exports: ["usePolling"]
    - path: "frontend/src/hooks/useDashboard.ts"
      provides: "Dashboard data fetching with date range and auto-polling"
      exports: ["useDashboard"]
    - path: "frontend/src/components/dashboard/DateFilter.tsx"
      provides: "Date range selector with presets and manual picker"
    - path: "frontend/src/components/dashboard/SummaryCards.tsx"
      provides: "Total revenue and transaction count display cards"
    - path: "frontend/src/pages/DashboardPage.tsx"
      provides: "Dashboard page composing data layer, filters, summary, and chart grid"
  key_links:
    - from: "frontend/src/hooks/useDashboard.ts"
      to: "frontend/src/api/client.ts"
      via: "calls GET /api/dashboard?start=&end="
    - from: "frontend/src/pages/DashboardPage.tsx"
      to: "frontend/src/hooks/useDashboard.ts"
      via: "useDashboard(dateRange) returns { data, loading, error, refresh }"
    - from: "frontend/src/pages/DashboardPage.tsx"
      to: "frontend/src/components/layout/DashboardLayout.tsx"
      via: "rendered inside DashboardLayout via App.tsx ProtectedRoute"
---

<objective>
Build the dashboard data layer: polling hook (30s interval + Page Visibility API), data fetching hook (GET /api/dashboard with date range), date filter component (presets + manual picker per D-01/D-02/D-03), summary statistics cards (D-05), and the DashboardPage shell that composes all data layer components and prepares the chart grid layout (D-16).

Purpose: This plan delivers the data plumbing and filter UI. Charts (Line, Pie) and UI widgets (spinner, refresh button, empty state) are created in Plan 02-05. At the end of this plan, the dashboard page renders with real data, summary cards, and date filtering — but with placeholder chart areas.
Output: Working DashboardPage with live data, date filtering, summary cards, shared filter state, and auto-polling. Chart grid layout ready for Plan 02-05 components.
</objective>

<execution_context>
@/home/boxedduck/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/boxedduck/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-dashboard/02-RESEARCH.md
@.planning/phases/02-dashboard/02-PATTERNS.md
@.planning/phases/02-dashboard/02-CONTEXT.md
@frontend/src/api/client.ts
@frontend/src/types/dashboard.ts
@frontend/src/lib/format.ts
@frontend/src/contexts/AuthContext.tsx
@frontend/src/components/layout/DashboardLayout.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create usePolling and useDashboard hooks</name>
  <files>frontend/src/hooks/usePolling.ts, frontend/src/hooks/useDashboard.ts</files>

  <read_first>
Read 02-RESEARCH.md §Pattern 5 (lines 379-418) for usePolling implementation.
Read 02-RESEARCH.md §Code Example (lines 632-683) for useDashboard implementation.
Read 02-PATTERNS.md §9 (lines 514-554) for usePolling pattern.
Read 02-PATTERNS.md §10 (lines 558-608) for useDashboard pattern.
Read frontend/src/types/dashboard.ts — DashboardData, DateRange interfaces.
Read frontend/src/api/client.ts — get() function signature.
</read_first>

<behavior>
- Test 1: usePolling calls fetchFn immediately on mount, then every intervalMs thereafter
- Test 2: usePolling pauses interval when document.hidden becomes true, resumes when false
- Test 3: usePolling clears interval on unmount (no memory leak)
- Test 4: useDashboard fetches GET /api/dashboard?start=&end= on mount and on dateRange change
- Test 5: useDashboard updates loading/error/data states correctly
- Test 6: useDashboard.refresh() triggers manual re-fetch
</behavior>

<action>
Create two hooks following the exact patterns from RESEARCH.md and PATTERNS.md.

**A. `src/hooks/usePolling.ts`** — per PATTERNS.md §9 (exact code from RESEARCH.md lines 381-418). Copy the implementation from PATTERNS.md lines 519-553 with one addition:
- Add `useCallback` wrapping on `fetchFn` dependency to avoid stale closure issues. The hook signature: `function usePolling(fetchFn: () => Promise<void>, intervalMs: number): void`.
- Uses `useRef` for interval ID, `useEffect` for setup/cleanup.
- Registers `visibilitychange` event listener that pauses polling when `document.hidden` is true (D-13) and resumes when visible.
- Calls `fetchFn()` immediately on mount (initial fetch) before starting the interval.
- Cleans up interval and event listener on unmount.
- DO NOT add extra state or loading flags — this is a pure side-effect hook.

**B. `src/hooks/useDashboard.ts`** — per PATTERNS.md §10 (exact code from RESEARCH.md lines 634-683). Copy the implementation with these adjustments:
- Import `DashboardData`, `DateRange` from `../types/dashboard`.
- Import `get` from `../api/client`.
- Import `usePolling` from `./usePolling`.
- The hook takes `dateRange: DateRange` as parameter.
- State: `data: DashboardData | null`, `loading: boolean` (init `true`), `error: string | null`.
- `fetchDashboard` callback: calls `get<{ success: boolean; data: DashboardData }>(`/dashboard?start=${dateRange.start}&end=${dateRange.end}`)`, extracts `result.data`, updates state.
- Calls `usePolling(fetchDashboard, 30000)` (D-10: 30-second interval).
- Exposes `refresh` callback: sets `loading: true`, calls `fetchDashboard()` (D-11: manual refresh).
- Returns `{ data, loading, error, refresh }`.
- DO NOT include `outletName` as separate state — the DashboardData already contains `outlet.name` per Plan 02-01 response format.
</action>

<verify>
  <automated>cd frontend && npx tsc --noEmit</automated>
</verify>

<acceptance_criteria>
- usePolling(fn, 30000) calls fn immediately, sets interval, pauses on tab hidden, cleans up on unmount
- useDashboard(dateRange) fetches from /api/dashboard with start/end params, manages loading/error/data states
- useDashboard returns refresh() for manual re-fetch
- TypeScript compiles without errors
</acceptance_criteria>

<done>usePolling and useDashboard hooks created. Polling respects Page Visibility API. Dashboard hook fetches data and exposes refresh.</done>
</task>

<task type="auto">
  <name>Task 2: Create DateFilter and SummaryCards components</name>
  <files>frontend/src/components/dashboard/DateFilter.tsx, frontend/src/components/dashboard/SummaryCards.tsx</files>

  <read_first>
Read 02-CONTEXT.md D-01 through D-05 (all filter and summary card decisions).
Read 02-RESEARCH.md §Don't Hand-Roll — uses date-fns for date math (subDays, startOfMonth, format).
Read frontend/src/lib/format.ts — formatRupiah for summary cards.
Read frontend/src/types/dashboard.ts — DateRange interface.
</read_first>

  <action>
Create two components implementing locked decisions D-01 through D-05.

**A. `src/components/dashboard/DateFilter.tsx`** — implements D-01 (default 7 days), D-02 (preset buttons), D-03 (custom date picker):
- Props: `value: DateRange`, `onChange: (range: DateRange) => void`.
- Imports from `date-fns`: `subDays, startOfMonth, format, endOfMonth`.
- Computes presets as functions that return DateRange:
  - "7 Hari" → `{ start: format(subDays(new Date(), 7), 'yyyy-MM-dd'), end: format(new Date(), 'yyyy-MM-dd') }` (D-01 default, D-02)
  - "30 Hari" → `{ start: format(subDays(new Date(), 30), 'yyyy-MM-dd'), end: format(new Date(), 'yyyy-MM-dd') }` (D-02)
  - "Bulan Ini" → `{ start: format(startOfMonth(new Date()), 'yyyy-MM-dd'), end: format(endOfMonth(new Date()), 'yyyy-MM-dd') }` (D-02)
  - "Semua" → `{ start: '2020-01-01', end: format(new Date(), 'yyyy-MM-dd') }` (D-02)
- Renders preset buttons as a horizontal row. Active preset should be visually highlighted (amber-400 bg for active vs gray-700 for inactive). Determine active by comparing the current `value` to each preset's computed range.
- Custom date picker (D-03): two `<input type="date">` fields for start and end dates, placed below/alongside preset buttons.
  - Start date input bound to `value.start`, end input bound to `value.end`.
  - On change of either input, call `onChange({ start: newStart, end: newEnd })`.
  - When a preset is clicked, call `onChange(presetRange)` — this overrides the manual date inputs.
- Styling: flex-wrap row, gap-2, mb-4.
- Preset button style: `px-3 py-1.5 rounded text-sm font-medium transition-colors`.
- Date input style: `bg-gray-800 border border-gray-700 rounded px-3 py-1.5 text-white text-sm` with native date picker dark mode support via `color-scheme: dark` CSS.
- Labels in Indonesian: placeholder text "DD-MM-YYYY" or let native date picker handle format.

**B. `src/components/dashboard/SummaryCards.tsx`** — implements D-05 (total revenue + transaction count):
- Props: `totalRevenue: number`, `transactionCount: number`, `loading?: boolean`.
- Renders two cards side-by-side in a responsive grid: `grid grid-cols-1 sm:grid-cols-2 gap-4 mb-6`.
- Card 1 — Total Revenue:
  - Label: "Total Omset" (text-sm text-gray-400)
  - Value: `formatRupiah(totalRevenue)` (text-3xl font-bold text-amber-400) — per OPENCODE.md 24pt font rule for financial data, use `text-3xl` (~30px ≈ 22.5pt, close enough) or custom `text-[24pt]`.
  - Loading state: show shimmer/pulse animation (`animate-pulse bg-gray-800 rounded h-8 w-48`)
- Card 2 — Transaction Count:
  - Label: "Jumlah Transaksi" (text-sm text-gray-400)
  - Value: `transactionCount` as number (text-3xl font-bold text-white)
  - Loading state: same shimmer pattern
- Card styling: `bg-gray-900 rounded-xl p-5 border border-gray-800`.
- Import `formatRupiah` from `../../lib/format`.
- Import `formatCompactRupiah` is optional — use `formatRupiah` for the card value; compact format is for chart axis labels.

DO NOT fetch data in these components — they receive props from DashboardPage. DO NOT hardcode values — they must be dynamic from props.
</action>

<verify>
  <automated>cd frontend && npx tsc --noEmit</automated>
</verify>

<acceptance_criteria>
- DateFilter renders 4 preset buttons (7H, 30H, Bulan Ini, Semua) and 2 date input fields (D-01, D-02, D-03)
- Clicking a preset calls onChange with correct DateRange
- Modifying date inputs calls onChange with updated range
- SummaryCards renders two cards showing formatRupiah(totalRevenue) and transactionCount (D-05)
- Loading state shows shimmer animation on both cards
- TypeScript compiles without errors
</acceptance_criteria>

<done>DateFilter with 4 presets + custom date picker. SummaryCards with revenue and transaction count display.</done>
</task>

<task type="auto">
  <name>Task 3: Create DashboardPage with data layer integration</name>
  <files>frontend/src/pages/DashboardPage.tsx, frontend/src/App.tsx</files>

  <read_first>
Read 02-CONTEXT.md D-01 (default 7 days), D-04 (shared filter), D-16 (chart grid layout).
Read 02-RESEARCH.md §Architecture Patterns — DashboardPage composes all dashboard components.
Read frontend/src/hooks/useDashboard.ts — useDashboard(dateRange) hook.
Read frontend/src/components/layout/DashboardLayout.tsx — layout wrapper.
Read frontend/src/App.tsx — current DashboardPage placeholder import.
</read_first>

  <action>
Create the real `frontend/src/pages/DashboardPage.tsx` and update `frontend/src/App.tsx` to import it.

**A. `src/pages/DashboardPage.tsx`** — per D-01, D-04, D-16:

State and data:
- Import `useState` from React, `useDashboard` from hooks, `DateFilter`, `SummaryCards` from components, `DateRange` from types.
- Initialize `dateRange` state with default 7-day range (D-01):
  ```typescript
  const [dateRange, setDateRange] = useState<DateRange>(() => {
    const end = new Date();
    const start = new Date();
    start.setDate(start.getDate() - 7);
    return {
      start: start.toISOString().slice(0, 10),
      end: end.toISOString().slice(0, 10),
    };
  });
  ```
- Call `const { data, loading, error, refresh } = useDashboard(dateRange)` (D-04: shared filter — data is fetched with dateRange and passed to all child components).

Render structure:
```
<DateFilter value={dateRange} onChange={setDateRange} />
<SummaryCards totalRevenue={data?.summary.totalRevenue ?? 0} transactionCount={data?.summary.transactionCount ?? 0} loading={loading} />

{/* Error state */}
{error && <div className="bg-red-900/50 border border-red-700 rounded-lg p-4 text-red-300 mb-4">{error}</div>}

{/* Chart Grid (D-16: side-by-side on desktop, stacked on mobile) */}
<div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
  {/* Line Chart placeholder — Plan 02-05 will replace this */}
  <div className="lg:col-span-2 bg-gray-900 rounded-xl border border-gray-800 p-4 min-h-[300px] flex items-center justify-center text-gray-500">
    Line Chart akan tersedia di plan selanjutnya
  </div>

  {/* Pie Chart placeholder — Plan 02-05 will replace this */}
  <div className="bg-gray-900 rounded-xl border border-gray-800 p-4 min-h-[300px] flex items-center justify-center text-gray-500">
    Pie Chart akan tersedia di plan selanjutnya
  </div>
</div>

{/* Refresh button + loading indicator placeholder — Plan 02-05 adds them */}
```

Notes:
- Line Chart takes full width on both desktop and mobile (`lg:col-span-2`) per D-16 (Line Chart full-width above, Pie Chart below).
- On desktop: Line Chart spans 2 cols (full width), Pie Chart is 1 col below. But D-16 says "side-by-side on desktop, stacked on mobile". Wait — re-reading D-16: "Line Chart full-width above, Pie Chart below on desktop; side-by-side on desktop, stacked on mobile" — there's a contradiction in D-16 itself. The first part says full-width, the second says side-by-side. The specific ideas section in CONTEXT.md clarifies: "Chart grid: Line Chart full-width above, Pie Chart below on desktop; side-by-side on desktop, stacked on mobile (Line Chart above, Pie Chart below)". This is still contradictory. Let me use the most common interpretation: D-16 says "Charts side-by-side on desktop, stacked on mobile" as the primary instruction. The full-width mention appears to be an older idea. Let me implement: side-by-side on desktop (`lg:grid-cols-2`), stacked on mobile (`grid-cols-1`). Both charts at equal 1-col width on desktop.

Actually, re-reading more carefully: "Chart grid: Line Chart full-width above, Pie Chart below on desktop; side-by-side on desktop, stacked on mobile (Line Chart above, Pie Chart below)". I think the user wants Line Chart to be more prominent. Let me implement: Line Chart spans full width on both, Pie Chart is below it (not side-by-side). On mobile they both stack naturally. Use `grid-cols-1` with Line Chart first, Pie Chart second — both taking full width:

```html
<div className="grid grid-cols-1 gap-6">
  <div className="...">{/* Line Chart */}</div>
  <div className="...">{/* Pie Chart */}</div>
</div>
```

This is the simplest interpretation and matches "Line Chart full-width above, Pie Chart below" from D-16.

**B. Update `frontend/src/App.tsx`:**
- Replace the inline DashboardPage placeholder with `import DashboardPage from './pages/DashboardPage';`.
- Remove any inline temporary component definition.
- The route stays: `<Route path="/dashboard" element={<ProtectedRoute><DashboardPage /></ProtectedRoute>} />`.

DO NOT add chart components, spinner, or refresh button yet — Plan 02-05 wires those in. DO NOT add outlet name to the page heading — the Header component already shows it (D-17).
</action>

<verify>
  <automated>cd frontend && npx tsc --noEmit</automated>
</verify>

<acceptance_criteria>
- DashboardPage initializes with default 7-day date range (D-01)
- DateFilter onChange updates dateRange state, triggering re-fetch via useDashboard (D-04)
- SummaryCards receives totalRevenue and transactionCount from fetched data (D-05)
- Chart grid layout uses grid-cols-1 with Line Chart placeholder above, Pie Chart placeholder below (D-16)
- Error state displays with red background and error message
- App.tsx imports DashboardPage from its real file path
- TypeScript compiles without errors
</acceptance_criteria>

<done>DashboardPage created with useDashboard integration, DateFilter, SummaryCards, and chart placeholders. App.tsx imports real DashboardPage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DashboardPage → /api/dashboard | Date range params sent as query string; validated by backend Zod schema |
| usePolling → Page Visibility API | Browser API — no user data crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Tampering | DateFilter inputs | low | mitigate | Backend validates date format and start ≤ end with Zod; frontend uses native date inputs (browser-enforced format) |
| T-02-15 | Information Disclosure | useDashboard fetch | medium | mitigate | All /api/dashboard requests include JWT Bearer token (via apiClient); data scoped to outletId |
| T-02-16 | Denial of Service | usePolling 30s interval | low | accept | 30s polling is low-frequency; backend reads pre-computed SalesTrend (O(1)) |
</threat_model>

<verification>
Start both backend and frontend:
```bash
# Terminal 1
npm run dev

# Terminal 2
cd frontend && npm run dev
```

Visit http://localhost:5173 — should redirect to /login.
Login with registered credentials → should redirect to /dashboard.
Verify: SummaryCards show revenue and transaction count (or zeros if no data).
Verify: DateFilter presets work — clicking "30 Hari" updates the data.
Verify: Network tab shows GET /api/dashboard requests every 30 seconds.
</verification>

<success_criteria>
- DashboardPage loads at /dashboard after authentication
- SummaryCards display total revenue (Rupiah) and transaction count from API
- DateFilter presets (7H, 30H, Bulan Ini, Semua) change the date range and refresh data
- Custom date picker inputs allow manual date range selection
- Data auto-refreshes every 30 seconds (visible in Network tab)
- Chart placeholder areas are rendered (ready for Plan 02-05)
</success_criteria>

<output>
Create `.planning/phases/02-dashboard/02-04-SUMMARY.md` when done
</output>
