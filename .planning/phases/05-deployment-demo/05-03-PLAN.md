---
phase: 05-deployment-demo
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - README.md
autonomous: true
requirements:
  - DEPLOY-03
must_haves:
  truths:
    - Root README.md exists and is written entirely in Bahasa Indonesia
    - README contains login instructions, dashboard overview, e-report export guide, and demo data injection steps
    - README includes deployment verification curl commands for quick smoke testing
    - Demo scenario documents the full owner journey from login to exported report
  artifacts:
    - README.md (project root)
  key_links:
    - README instructions <-> actual UI labels and page flows in the codebase
    - Demo scenario <-> POST /api/admin/dummy-inject endpoint behavior
---

<objective>
Produce a user-facing onboarding guide in Bahasa Indonesia at the project root. The guide must enable a restaurant owner to log in, view the dashboard, export reports, and inject demo data without developer assistance. Include deployment verification commands.

Purpose: D-63 and D-64 require a short user guide in Bahasa Indonesia for restaurant owners. The existing frontend/README.md is only a Vite scaffold template.
Output: README.md covering the complete owner journey and demo scenario.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deployment-demo/05-CONTEXT.md

frontend/README.md — currently a generic Vite + React scaffold document (32 lines)
No root README.md exists yet
Existing features to document: login, dashboard (Line Chart + Pie Chart), E-Report (PDF/CSV export), Suntik Data Simulasi
UI is dark mode with amber accents, 24pt font for financial data
Auth: localStorage token, Bearer header
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write root README.md user guide in Bahasa Indonesia</name>
  <files>README.md</files>
  <read_first>
    - frontend/src/App.tsx (to understand routes)
    - frontend/src/pages/DashboardPage.tsx or equivalent (to document dashboard features)
    - frontend/src/pages/EReportPage.tsx or equivalent (to document report features)
  </read_first>
  <action>
    Create README.md in the project root (not inside frontend/). Write entirely in Bahasa Indonesia. Structure:
    1. # RestoPulse — panduan singkat untuk pemilik restoran
    2. ## Apa itu RestoPulse? (one paragraph describing the app purpose per PROJECT.md core value)
    3. ## Cara Login (step-by-step: buka URL aplikasi, masukkan username dan password yang telah didaftarkan, klik tombol Login, tunggu redirect ke Dasbor)
    4. ## Melihat Dasbor (explain: pilih rentang tanggal dengan preset 7/30/90 hari atau kustom, lihat Line Chart tren omset harian, lihat Pie Chart persentase menu terlaris, sentuh titik grafik untuk melihat tooltip detail)
    5. ## Mengekspor Laporan (buka halaman E-Report, pilih rentang tanggal, klik tombol Export PDF atau Export CSV, file akan diunduh otomatis)
     6. ## Menyuntik Data Demo (buka halaman Data Entry, klik tombol "Suntik Data Simulasi", ikuti dialog konfirmasi yang muncul sesuai implementasi yang sudah ada, tunggu proses generate data selesai)
    7. ## Verifikasi Deployment (curl commands: curl -s https://<backend-url>/health and curl -s https://<backend-url>/api/auth/register with example JSON body)
    Each section must be at least 2 sentences. No screenshots required.
  </action>
  <verify>
    <automated>
      Test-Path README.md
      (Get-Content README.md | Measure-Object -Line).Lines -ge 40
      grep -c '## Cara Login' README.md | Select-String "1"
      grep -c '## Melihat Dasbor' README.md | Select-String "1"
      grep -c '## Mengekspor Laporan' README.md | Select-String "1"
      grep -c '## Menyuntik Data Demo' README.md | Select-String "1"
      grep -c 'curl' README.md | Select-String "2"
    </automated>
  </verify>
  <done>
    README.md exists at project root with all 5 required sections in Bahasa Indonesia.
  </done>
  <acceptance_criteria>
    - `README.md` exists at project root
    - Contains exactly these H2 sections: `## Cara Login`, `## Melihat Dasbor`, `## Mengekspor Laporan`, `## Menyuntik Data Demo`, `## Verifikasi Deployment`
    - At least 40 lines total
    - At least 2 `curl` examples in the Verifikasi Deployment section
    - No English sentences in user-facing sections (technical terms like "PDF", "CSV", "URL", "API" are acceptable as loanwords)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Demo scenario and endpoint verification</name>
  <files>README.md</files>
  <read_first>
    - src/routes/admin.routes.ts (to confirm dummy-inject route path and method)
    - src/app.ts (to confirm /health and /api/auth routes)
  </read_first>
   <action>
    In README.md, add a "Skenario Demo" subsection under "Menyuntik Data Demo" with a numbered list of the exact steps a presenter should follow:
    1. Daftarkan akun baru melalui halaman login (atau gunakan akun existing)
    2. Login dan perhatikan Dasbor Utama kosong
    3. Buka Data Entry, klik "Suntik Data Simulasi", ikuti alur konfirmasi sesuai implementasi yang sudah ada di aplikasi
    4. Tunggu notifikasi sukses, lalu kembali ke Dasbor
    5. Perhatikan Line Chart dan Pie Chart kini menampilkan data simulasi
    6. Buka E-Report, pilih rentang "Bulanan", klik Export PDF
    Before writing the scenario, read the existing dummy injector implementation (frontend Data Entry page and/or backend admin controller) to document the exact confirmation dialog flow as implemented in Phase 3. Do not invent or suggest UI changes — only document what already exists. Verify that the README mentions the exact endpoint path `POST /api/admin/dummy-inject`. Also verify the health check curl command uses the exact `/health` path.
  </action>
  <verify>
    <automated>
      grep -c 'Skenario Demo' README.md | Select-String "1"
      grep -c 'POST /api/admin/dummy-inject' README.md | Select-String "1"
      grep -c '/health' README.md | Select-String "1"
    </automated>
  </verify>
  <done>
    README.md contains a numbered demo scenario that documents the existing confirmation dialog flow as implemented in Phase 3, and references the exact dummy-inject endpoint.
  </done>
  <acceptance_criteria>
    - `grep 'Skenario Demo' README.md` returns 1 match
    - `grep 'POST /api/admin/dummy-inject' README.md` returns 1 match
    - `grep '/health' README.md` returns at least 1 match
    - The scenario step for dummy injection describes the confirmation dialog using the exact wording/flow from the existing implementation (not invented text)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> user | Public documentation crosses from developer to end-user trust domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Information Disclosure | README curl examples | low | accept | Examples use placeholder URLs; no real credentials or secrets are shown |
| T-05-08 | Repudiation | Missing demo scenario | low | accept | Demo scenario is documented for reproducible presentations |
</threat_model>

<verification>
- `Test-Path README.md` is true
- README contains all 5 required sections
- README references `POST /api/admin/dummy-inject` and `HAPUS`
- README is written in Bahasa Indonesia
</verification>

<success_criteria>
- Root README.md exists with user guide in Bahasa Indonesia
- All 4 owner-facing features are documented
- Demo scenario is numbered and reproducible
- Deployment verification curl examples are present
</success_criteria>

<output>
Create `.planning/phases/05-deployment-demo/05-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `README.md` — project root user guide in Bahasa Indonesia with sections for Login, Dashboard, E-Report Export, Demo Data Injection, and Deployment Verification
