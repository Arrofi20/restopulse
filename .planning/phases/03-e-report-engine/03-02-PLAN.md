---
phase: 03-e-report-engine
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - frontend/src/types/report.ts
  - frontend/src/hooks/useReport.ts
  - frontend/src/hooks/__tests__/useReport.test.ts
  - frontend/src/components/report/ReportDateFilter.tsx
  - frontend/src/components/report/ReportSummaryCards.tsx
  - frontend/src/components/report/ReportDailyTable.tsx
  - frontend/src/components/report/ExportButtons.tsx
  - frontend/src/pages/EReportPage.tsx
autonomous: true
requirements:
  - REPT-01

must_haves:
  truths:
    - Owner can select Harian/Mingguan/Bulanan/Custom date range on the E-Report page
    - Report preview displays outlet name, report period, total revenue, transaction count, top menu items, and daily breakdown table
    - Export buttons are sticky at the top-right of the preview and full-width on mobile
    - Report date state is independent of dashboard date state (D-21)
  artifacts:
    - frontend/src/types/report.ts
    - frontend/src/hooks/useReport.ts
    - frontend/src/components/report/ReportDateFilter.tsx
    - frontend/src/components/report/ReportSummaryCards.tsx
    - frontend/src/components/report/ReportDailyTable.tsx
    - frontend/src/components/report/ExportButtons.tsx
    - frontend/src/pages/EReportPage.tsx
  key_links:
    - useReport hook -> GET /api/report -> ReportController
    - EReportPage -> ReportDateFilter (independent date state)
    - EReportPage -> ExportButtons (sticky, mobile responsive)
    - EReportPage -> ReportSummaryCards + ReportDailyTable (preview)
---

<objective>
Build the E-Report preview UI page with independent date filtering, summary statistics, daily breakdown table, and placeholder export buttons.

Purpose: Provides the report preview screen that owners see before exporting to PDF or CSV.
Output: Fully functional /e-report page with report data fetching, date filtering, summary cards, daily table, and sticky export button area.
</objective>

<execution_context>
@/home/boxedduck/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/boxedduck/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-e-report-engine/03-CONTEXT.md
@.planning/phases/03-e-report-engine/03-RESEARCH.md
@frontend/src/types/dashboard.ts
@frontend/src/hooks/useDashboard.ts
@frontend/src/hooks/__tests__/useDashboard.test.ts
@frontend/src/components/dashboard/DateFilter.tsx
@frontend/src/components/dashboard/SummaryCards.tsx
@frontend/src/lib/format.ts
@frontend/src/api/client.ts
@frontend/src/pages/EReportPage.tsx
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create report types, useReport hook, and ReportDateFilter</name>
  <files>frontend/src/types/report.ts, frontend/src/hooks/useReport.ts, frontend/src/hooks/__tests__/useReport.test.ts, frontend/src/components/report/ReportDateFilter.tsx</files>
  <read_first>frontend/src/types/dashboard.ts, frontend/src/hooks/useDashboard.ts, frontend/src/hooks/__tests__/useDashboard.test.ts, frontend/src/components/dashboard/DateFilter.tsx</read_first>
  <action>
    Create report TypeScript types, a data fetching hook, and an adapted date filter component.

    **frontend/src/types/report.ts:**
    - `ReportRow` interface: `{ date: string; revenue: number; topMenu: string; transactionCount: number }`
    - `ReportData` interface: `{ outlet: { name: string }; period: { start: string; end: string }; summary: { totalRevenue: number; transactionCount: number; topItems: string[] }; rows: ReportRow[] }`
    - `ReportResponse` interface: `{ success: boolean; data: ReportData }`
    - Re-export `DateRange` from `./dashboard` for convenience.

    **frontend/src/hooks/useReport.ts:**
    - Mirrors `useDashboard` exactly but calls `GET /report?start=&end=` instead of `/dashboard`.
    - Returns `{ data: ReportData | null, loading: boolean, error: string | null, refresh: () => void }`.
    - Uses `usePolling(fetchReport, 30000)` for auto-refresh (same pattern as dashboard).
    - Date range is independent — this hook owns its own date state internally via `useState` with `defaultReportDateRange()`.
    - Export `useReport()` hook.

    **frontend/src/hooks/__tests__/useReport.test.ts:**
    - Mirror the useDashboard.test.ts pattern with 4 behavioral tests:
      1. Fetches GET /api/report?start=&end= on mount
      2. Updates loading/error/data states correctly
      3. refresh() triggers manual re-fetch
      4. Sets error state when fetch rejects
    - Mock `../../api/client` get function.

    **frontend/src/components/report/ReportDateFilter.tsx:**
    - Copy-adapt the DateFilter component pattern with report-specific presets per D-19.
    - Presets: `Harian` (today), `Mingguan` (last 7 days), `Bulanan` (current month), `Custom` (manual picker).
    - `defaultReportDateRange()` returns the Bulanan preset (D-20).
    - Use `date-fns` `format`, `subDays`, `startOfMonth`, `endOfMonth` for timezone-safe date math.
    - Same UI structure as DateFilter: preset buttons + two native date inputs with `[color-scheme:dark]`.
    - Active preset highlighted with amber-400 bg.
    - Props: `{ value: DateRange; onChange: (range: DateRange) => void }`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>useReport.ts and ReportDateFilter.tsx compile. useReport.test.ts has 4 behavioral tests. ReportDateFilter exports defaultReportDateRange() returning current month range. All presets use date-fns local-date format.</done>
</task>

<task type="auto">
  <name>Task 2: Create ReportSummaryCards and ReportDailyTable components</name>
  <files>frontend/src/components/report/ReportSummaryCards.tsx, frontend/src/components/report/ReportDailyTable.tsx</files>
  <read_first>frontend/src/components/dashboard/SummaryCards.tsx, frontend/src/lib/format.ts</read_first>
  <action>
    Create the two report preview sub-components.

    **frontend/src/components/report/ReportSummaryCards.tsx:**
    - Props: `{ data: ReportData | null; loading: boolean }`.
    - Renders 3 cards in a responsive grid (`grid grid-cols-1 gap-4 sm:grid-cols-3`):
      1. "Total Omset" — `formatRupiah(summary.totalRevenue)` in `text-3xl text-amber-400` (>=24pt per OPENCODE.md).
      2. "Jumlah Transaksi" — `summary.transactionCount` in `text-3xl text-white`.
      3. "Menu Terlaris" — `summary.topItems.slice(0, 3).join(', ')` in `text-lg text-white` (or "-" if empty).
    - Loading state: `animate-pulse` shimmer placeholders (same pattern as SummaryCards).
    - When `data` is null and not loading, show "-" for all values.

    **frontend/src/components/report/ReportDailyTable.tsx:**
    - Props: `{ rows: ReportRow[]; loading: boolean }`.
    - Wrap table in `div className="overflow-x-auto"` for mobile horizontal scroll.
    - Table columns: Tanggal, Omset (Rp), Menu Terlaris, Jumlah Transaksi.
    - Header row: `bg-gray-800 text-gray-300`, left-aligned Tanggal/Menu, right-aligned Omset, center-aligned Jumlah Transaksi.
    - Body rows: `divide-y divide-gray-800`, alternating subtle bg (`even:bg-gray-900/50`).
    - Omset column uses `formatRupiah(row.revenue)`.
    - Loading state: 5 skeleton rows with `animate-pulse` gray bars.
    - Empty state (rows.length === 0 and not loading): single row with colspan=4 showing "Tidak ada data untuk periode ini".
    - Per D-23, shows daily breakdown table.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>ReportSummaryCards.tsx and ReportDailyTable.tsx compile. Summary cards show total revenue (formatRupiah), transaction count, and top 3 menu items. Daily table shows scrollable rows with Rupiah formatting. Loading and empty states are handled.</done>
</task>

<task type="auto">
  <name>Task 3: Build EReportPage with preview layout and ExportButtons placeholder</name>
  <files>frontend/src/components/report/ExportButtons.tsx, frontend/src/pages/EReportPage.tsx</files>
  <read_first>frontend/src/pages/EReportPage.tsx, frontend/src/components/layout/DashboardLayout.tsx, frontend/src/hooks/useDashboard.ts, frontend/src/components/dashboard/SummaryCards.tsx</read_first>
  <action>
    Replace the EReportPage placeholder with a full report preview page, and create the ExportButtons component with placeholder handlers.

    **frontend/src/components/report/ExportButtons.tsx:**
    - Props: `{ data: ReportData | null; onExportPDF?: () => void; onExportCSV?: () => void }`.
    - Render a sticky container: `sticky top-0 z-10 bg-gray-950/95 backdrop-blur-sm py-3`.
    - Inside: `flex flex-col gap-2 sm:flex-row sm:justify-end`.
    - Two buttons:
      - "Export PDF" — `bg-amber-400 text-black font-bold`, disabled when `!data` or `!onExportPDF`.
      - "Export CSV" — `bg-gray-700 text-white`, disabled when `!data` or `!onExportCSV`.
    - On mobile (`w-full` below sm), buttons are full-width per D-35.
    - On desktop (`sm:w-auto`), buttons sit at the right.
    - For now, `onExportPDF` and `onExportCSV` are optional; if not provided, buttons are disabled. This allows 03-03 and 03-04 to wire handlers without modifying the component's core layout.

    **frontend/src/pages/EReportPage.tsx:**
    - Import and use `useReport()` hook (owns its own date state — D-21 independent).
    - Import `ReportDateFilter`, `ReportSummaryCards`, `ReportDailyTable`, `ExportButtons`.
    - Page layout (top to bottom):
      1. Page header: "E-Report" in `text-2xl font-bold text-white mb-4`.
      2. Outlet name + period label: `data ? \`${data.outlet.name} — ${data.period.start} s/d ${data.period.end}\` : 'Memuat...'` in `text-sm text-gray-400 mb-4`.
      3. `ReportDateFilter` with `value={dateRange} onChange={setDateRange}`.
      4. `ExportButtons` with `data={data}` (no handlers yet — buttons disabled).
      5. `ReportSummaryCards` with `data={data} loading={loading}`.
      6. Error banner when `error`: red bg, white text, `error` message.
      7. `ReportDailyTable` with `rows={data?.rows ?? []} loading={loading}`.
    - The entire content is inside DashboardLayout via ProtectedRoute (already in App.tsx).
    - Per D-34, content stacks vertically on mobile naturally via flex-col.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>EReportPage.tsx replaces the placeholder and compiles. Page composes useReport, ReportDateFilter, ExportButtons, ReportSummaryCards, and ReportDailyTable. Export buttons are sticky, disabled until handlers are wired. npm run build (tsc -b && vite build) exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage → API | Bearer token stored in localStorage; 401 triggers redirect |
| Client → Report API | Date ranges sent to backend; validated by Zod schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Information Disclosure | useReport hook | medium | mitigate | Uses existing apiClient which attaches Bearer token and handles 401 redirect; no token logging |
| T-03-06 | Tampering | DateFilter inputs | low | accept | Native date inputs enforce format; backend re-validates with Zod. No additional frontend validation needed beyond existing patterns. |
| T-03-07 | Denial of Service | 30s polling on report page | low | accept | Same pattern as dashboard (D-10); usePolling pauses on tab hidden (D-13). Pre-computed SalesTrend reads are O(1). |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0
- EReportPage replaces the placeholder — `grep -c "E-Report akan tersedia" frontend/src/pages/EReportPage.tsx` returns 0
- `grep -c "defaultReportDateRange" frontend/src/components/report/ReportDateFilter.tsx` returns >= 1
- `grep -c "useReport" frontend/src/pages/EReportPage.tsx` returns >= 1
- `grep -c "ExportButtons" frontend/src/pages/EReportPage.tsx` returns >= 1
- ReportDateFilter has 4 presets: Harian, Mingguan, Bulanan, and custom date inputs
</verification>

<success_criteria>
- Visiting /e-report shows the report preview page with date filter, summary cards, and daily table
- Default date range is current month (Bulanan)
- Date filter presets compute correct ranges via date-fns
- Report data fetches from GET /api/report independently of dashboard state
- Export buttons are sticky at the top and full-width on mobile
- npm run build exits 0 with no TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/03-e-report-engine/03-02-SUMMARY.md` when done
</output>
